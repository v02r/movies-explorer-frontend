

const More = ({handleMore}) => {
    return (
        <button type="button" className="cards__button text link" onClick={handleMore}>
            Ещё
        </button>
    )
}

export default More;
